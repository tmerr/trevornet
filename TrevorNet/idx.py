"""Parse an IDX file like the one used in the MNIST handwritten digit database"""

def idx_to_list(fname):
    """Parse the IDX file and return an n dimensional list."""
    with open(fname) as f:
        pass
